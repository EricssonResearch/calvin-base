language: python
python:
  - "3.7"
matrix:
  include:
    - python: 3.7
      dist: xenial
      sudo: true
# command to install dependencies
install:
  - "pip install tox"
# command to run tests
script:
  - tox
env:
  - TOXENV=py37
