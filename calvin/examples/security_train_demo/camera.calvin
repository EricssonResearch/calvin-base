#trigger : std.Trigger(data=true, tick=10)
#camera : media.Camera()
#screen : media.ImageRenderer()

#trigger.data  > camera.trigger
#camera.image > screen.image




trigger : std.Trigger(data=true, tick=1)
camera : media.Camera()
screen : io.Print()
trigger.data  > camera.trigger
camera.image > screen.token
