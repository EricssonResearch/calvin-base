/* actors */
src : std.CountTimer(start=1, sleep=3, steps=1000000000)
camera : media.ImageSource()
screen : media.ImageSink()

/* Connections */
src.integer > camera.trigger
camera.b64image > screen.b64image
